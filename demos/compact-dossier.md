SYSTEM CONTRACT (stable reference, must never be dropped)
You are the delivery analyst. Answer strictly from the evidence in this dossier.

<!-- lcc:cache-break -->

The clinic booking widget loses 63 percent of mobile visitors before the second step of the form.

TOOL OUTPUT 1: the scanner returned HTTP 200 with 41 kB of HTML, no console errors, the viewport meta tag is present and 12 third-party scripts block first paint.

TOOL OUTPUT 2: the scanner returned HTTP 200 with 41 kB of HTML, no console errors, the viewport meta tag is present and 12 third-party scripts block first paint.

LOG 1: npm WARN deprecated legacy-form-widget@1.0.1 this package is no longer maintained and will be removed in a future release. Please migrate to a supported alternative.

LOG 2: queue worker heartbeat ok in 4821 ms, backlog 287 jobs, retry budget untouched, no dead letters in the last window.

CHATTER 1: someone mentioned in passing that we should probably look at this again later, not urgent, just noting it here so it does not get lost in the thread somewhere.

npm WARN deprecated legacy-form-widget@1.0.2 this package is no longer maintained and will be removed in a future release.

npm WARN deprecated legacy-form-widget@1.0.3 this package is no longer maintained and will be removed in a future release.

OPERATING CONSTRAINT: an appointment may only be rebooked through the front desk, and only within 48 hours of the original slot.

TOOL OUTPUT 3: lighthouse run scored performance 61 out of 100 on mobile emulation with 6 render-blocking resources.

CHATTER 2: someone asked whether the staging credentials still work and nobody answered, so treat this line as unresolved background rather than evidence.

REVISION: the p75 figure was revised on 3 September, down from 5.8 seconds measured in June.

p75 mobile load time on the treatment landing page is 4.2 seconds on a throttled 3G profile.

COMPLIANCE RULE: patients who have not signed the updated consent form must not be contacted about the booking system migration.

CHATTER 3: this might be worth revisiting at some point, not urgent, just leaving a note so the detail does not get lost somewhere in the thread.

npm WARN deprecated legacy-form-widget@1.0.4 this package is no longer maintained and will be removed in a future release.

EXCEPTION: the 63 percent abandonment figure excludes returning customers entirely, so it describes first-time visitors only.

TOOL OUTPUT 4: crawl pass found 38 internal links, 4 broken, 2 redirect chains longer than 3 hops.

CONFLICTING MEASUREMENT: a later export puts monthly churn at 8.7 percent, against the 4.2 percent recorded earlier in the same quarter.

Front desk currently re-types every booking into a paper diary, averaging 4 minutes per booking.

CHATTER 4: the coffee machine repair needs a part and the plants need water, filed here for no particular reason at all.
