services:
  lcc-router:
    build: .
    env_file:
      - .env
    command:
      - lcc
      - route
      - eval
      - --cases
      - examples/tasks
      - --output
      - eval/reports/docker_eval.json
